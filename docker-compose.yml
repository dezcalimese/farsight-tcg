services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: farsight
      POSTGRES_PASSWORD: farsight
      POSTGRES_DB: farsight
    ports:
      - "5432:5432"
    volumes:
      - farsight_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U farsight"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  farsight_postgres_data:
